import bpy

from vox_exporter.operators.voxel.base_operator_importer import BaseOperatorImporter

class IMPORT_OT_{{code_name}}(BaseOperatorImporter):
    bl_idname = "import.{{code_name}}"
    bl_label = "{{name}} (.{{type}})"
    bl_description = "Import {{name}} format (.{{type}})"
    bl_options = {'REGISTER', 'UNDO'}

    filename_ext = ".{{type}}"

    filter_glob: bpy.props.StringProperty(
        default="*.{{type}}",
        options={'HIDDEN'},
        maxlen=255,
    )

    def execute(self, context):
        # Your import logic goes here
        return {'FINISHED'}
